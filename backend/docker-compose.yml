version: '3.8'

services:
  myaicoach-voice-gateway:
    build: .
    container_name: myaicoach_voice_gateway
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
